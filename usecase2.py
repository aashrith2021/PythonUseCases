#example for operations that can be performed on a python tuple

names_of_states=('Goa','Orissa','Rajasthan','Karnataka')       #declaring a Tuple

#Adding and removing elements to the tuple is not possible as tuple is an immutable data structure

for state in names_of_states:
    print(state)